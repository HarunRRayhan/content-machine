<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            No DB_CONNECTION override here on purpose. This project's tests rely on
            Postgres-specific behavior (JSONB columns, partial unique indexes, CHECK
            constraints applied only under the pgsql driver, row-level locking in
            ReserveContentIdAction via lockForUpdate) that SQLite either ignores or
            emulates differently. A hardcoded sqlite/:memory: override here previously
            made every local/CI test run silently ignore whatever DB_CONNECTION .env
            actually specified; CI's postgres:17 service and migrations were real,
            but the test run itself was against SQLite the whole time. Tests now use
            whatever DB_CONNECTION is in .env: point it at a real Postgres locally
            (see any recent PR's verification steps for the throwaway-container
            pattern), same as CI's own "Configure database environment" step does.
        -->
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
