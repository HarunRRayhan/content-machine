## What does this change?

<!-- One or two sentences. What, and why. -->

## Checklist

- [ ] Tests added or updated for the change (or explained why none are needed)
- [ ] `./vendor/bin/pint --test`, `./vendor/bin/phpstan analyse`, and `./vendor/bin/pest --parallel` pass locally
- [ ] `npm run lint:check`, `npm run format:check`, `npx tsc --noEmit`, and `npm run build` pass locally
- [ ] Any database migration in this PR is additive-only (expand/contract — it does not drop or rename a column that's still in use)

## Anything reviewers should look at closely?

<!-- Optional: tricky parts, things you're unsure about, follow-ups you're deliberately deferring. -->
