<!-- neural net: "a model" as an object, in the library's node language (hollow circles, edges that
     stop short of them, same weights as icon-pipeline / icon-git-merge). Three inputs fanning into
     one output: at 24px that still reads as a network, where a full 2-2-1 mesh closes up into a
     solid blob. Sibling to icon-pipeline (stages in a row) -- this one is a model, not a process. -->
<svg viewBox="0 0 24 24"><circle cx="5" cy="5" r="2.3"/><circle cx="5" cy="12" r="2.3"/><circle cx="5" cy="19" r="2.3"/><circle cx="18.5" cy="12" r="2.3"/><path d="M7.6 6.3 15.9 10.7M7.9 12h7.7M7.6 17.7 15.9 13.3"/></svg>
