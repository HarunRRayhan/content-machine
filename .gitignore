/.phpunit.cache
/bootstrap/ssr
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder
/vendor
.DS_Store
.env
.env.backup
.env.production

# Operational/infra credentials (Railway tokens, etc.) — see .secrets/README.md.
# Deliberately separate from the app's own .env: a local test run copying a
# throwaway env over .env has, in practice, wiped out infra tokens that had
# nothing to do with the app itself.
/.secrets/*.env
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

.worktrees/
